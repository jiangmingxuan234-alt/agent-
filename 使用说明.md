# 专项多 Agent 工作流使用说明

本文档适用于当前这台 Windows 电脑，所有项目和运行目录都位于 C 盘。

## 1. 这套工作流能做什么

提交一条软件开发需求后，工作流会依次完成：

1. `gpt-5.6-terra` 分析需求并制定实施计划。
2. Codex CLI 使用 `gpt-5.6-sol` 编写和修改代码。
3. 本地程序执行真实的构建或测试命令。
4. `claude-sonnet-4-6` 更新文档。
5. `gpt-5.6-luna` 独立审查代码、测试和文档。
6. 工作流暂停，等待人工查看差异并批准或拒绝。
7. 批准后只在隔离分支中创建提交，不会自动合并或上传。

## 2. 当前目录

工作流程序：

```text
C:\Users\win11\Documents\specialist-agent-workflow
```

当前目标项目 `text-to-cae`：

```text
C:\Users\win11\Documents\Codex\2026-05-31\cai-aa-text-to-cae-https\work\text-to-cae
```

Agent 隔离工作区：

```text
C:\agent-runtime\worktrees
```

工作流检查点：

```text
C:\Users\win11\.specialist-agent-workflow\checkpoints.sqlite
```

## 3. 首次运行前检查

打开一个新的 PowerShell，先检查 Git：

```powershell
git --version
where.exe git
```

如果显示“无法识别 git”或工作流出现下面的错误：

```text
FileNotFoundError: [WinError 2] 系统找不到指定的文件
During task with name 'prepare'
```

说明 Windows PowerShell 找不到 Git。执行：

```powershell
winget install --id Git.Git -e --source winget
```

安装完成后关闭 PowerShell，再重新打开，然后确认：

```powershell
git --version
```

如果 Git 已安装在默认位置，但当前窗口还没有识别，可以临时执行：

```powershell
$env:Path = "C:\Program Files\Git\cmd;$env:Path"
git --version
```

接着检查工作流配置：

```powershell
Set-Location "C:\Users\win11\Documents\specialist-agent-workflow"
.\.venv\Scripts\specialist-workflow.exe doctor
```

正常情况下会显示模型、Codex 路径和 `api_key: available (hidden)`，不会显示真实密钥。

## 4. 运行第一个任务

建议每次都设置一个容易记住且不重复的 `--thread-id`。下面的命令可直接运行：

```powershell
Set-Location "C:\Users\win11\Documents\specialist-agent-workflow"

.\.venv\Scripts\specialist-workflow.exe run `
  --thread-id "milling-fix-001" `
  --repo "C:\Users\win11\Documents\Codex\2026-05-31\cai-aa-text-to-cae-https\work\text-to-cae" `
  --request "修复 milling-3d 仿真结果网格加载时出现 400 Bad Request 的问题，并更新相关测试和文档" `
  --test-command "npm.cmd --prefix viewer run build"
```

说明：

- `--thread-id`：本次任务编号。建议只使用英文、数字和连字符。
- `--repo`：需要 Agent 修改的 Git 项目目录，可以位于 C 盘任意位置。
- `--request`：用自然语言描述开发任务、验收要求和禁止事项。
- `--test-command`：完成修改后必须通过的本地测试或构建命令。

运行可能需要几分钟。不要在模型调用或测试执行期间关闭窗口。正常完成后会显示 `Thread ID`，并停在人工审批阶段。

## 5. 怎样写任务需求

一条清楚的需求最好同时说明问题、期望结果、修改范围和验收方式。例如：

```text
修复 milling-3d 仿真结果网格加载时出现 400 Bad Request 的问题。
保留现有其他案例的加载行为，不修改 API 对外字段。
补充能够覆盖该问题的测试，并更新 README 中的运行说明。
最终必须通过 viewer 构建。
```

其他例子：

```powershell
.\.venv\Scripts\specialist-workflow.exe run `
  --thread-id "viewer-loading-001" `
  --repo "C:\Users\win11\Documents\Codex\2026-05-31\cai-aa-text-to-cae-https\work\text-to-cae" `
  --request "为结果查看器增加加载失败提示和重试按钮，保持现有界面风格，并补充测试和文档" `
  --test-command "npm.cmd --prefix viewer run build"
```

每个新任务都使用新的 `--thread-id`，例如 `viewer-loading-002`。不要对已有检查点重复执行同一个 `run` 命令。

## 6. 查看任务状态和修改内容

先进入工作流目录：

```powershell
Set-Location "C:\Users\win11\Documents\specialist-agent-workflow"
$ThreadId = "milling-fix-001"
```

查看当前状态：

```powershell
.\.venv\Scripts\specialist-workflow.exe status $ThreadId
```

查看 Agent 产生的完整代码差异：

```powershell
.\.venv\Scripts\specialist-workflow.exe inspect $ThreadId
```

重点检查：

- 是否真正解决了原始问题。
- 是否修改了不相关文件。
- 测试或构建是否通过。
- Reviewer 是否报告高风险问题。
- 文档内容是否与代码一致。

`status` 输出中的 `branch_name` 是隔离分支名，`worktree_path` 是 Agent 实际修改代码的位置。

## 7. 批准或拒绝

确认修改正确后批准：

```powershell
.\.venv\Scripts\specialist-workflow.exe resume $ThreadId --approve
```

批准后，工作流会在 `agent/<任务ID>` 隔离分支中创建 Git 提交，但不会自动合并到 `main`。

不接受修改时拒绝：

```powershell
.\.venv\Scripts\specialist-workflow.exe resume $ThreadId --reject
```

`--approve` 和 `--reject` 必须二选一，不能同时使用。

## 8. 把批准的代码合并到主分支

批准后先查看分支和提交：

```powershell
Set-Location "C:\Users\win11\Documents\Codex\2026-05-31\cai-aa-text-to-cae-https\work\text-to-cae"
git status --short
git branch --list "agent/*"
git log --oneline --all --decorate -10
```

从 `status` 输出找到准确的 `branch_name`，然后合并。下面的分支名只是示例：

```powershell
git switch main
git merge "agent/实际任务ID"
```

合并后再次执行项目验证：

```powershell
npm.cmd --prefix viewer run build
```

确认无误后，可查看隔离工作区：

```powershell
git worktree list
```

不要在审批前直接编辑或删除 `C:\agent-runtime\worktrees` 下的目录。

## 9. 常用测试命令

当前 `text-to-cae` 查看器：

```powershell
npm.cmd --prefix viewer run build
```

Python 项目：

```powershell
python -m pytest
```

根目录包含 `package.json` 的 Node.js 项目：

```powershell
npm.cmd test -- --run
```

Rust 项目：

```powershell
cargo test
```

Go 项目：

```powershell
go test ./...
```

如果项目的 `package.json` 位于子目录，必须像当前项目一样显式传入 `--test-command`。

## 10. 常见问题

### 10.1 `FileNotFoundError: [WinError 2]`

本次日志中的错误发生在运行 `git rev-parse` 时，原因是普通 PowerShell 的 `PATH` 中没有 Git。按第 3 节安装 Git，重新打开 PowerShell后再运行。上一次命令没有进入编码阶段，因此可使用一个新的 `--thread-id` 重新提交。

### 10.2 `The source repository has uncommitted changes`

工作流要求源项目处于干净状态。先查看：

```powershell
Set-Location "C:\Users\win11\Documents\Codex\2026-05-31\cai-aa-text-to-cae-https\work\text-to-cae"
git status
```

确认修改后自行提交：

```powershell
git add -A
git commit -m "保存运行工作流前的修改"
```

不要为了运行工作流而丢弃尚未确认的个人修改。

### 10.3 `Repository path does not exist`

检查 `--repo` 是否为真实目录，并保留路径两侧的英文双引号：

```powershell
Test-Path "C:\Users\win11\Documents\Codex\2026-05-31\cai-aa-text-to-cae-https\work\text-to-cae"
```

返回 `True` 才表示路径存在。

### 10.4 `Could not detect tests`

工作流没有在项目根目录识别出测试框架。重新运行任务时显式添加：

```powershell
--test-command "npm.cmd --prefix viewer run build"
```

### 10.5 PowerShell 中文乱码

在当前 PowerShell 窗口执行：

```powershell
chcp 65001
[Console]::InputEncoding = [System.Text.UTF8Encoding]::new()
[Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()
$OutputEncoding = [Console]::OutputEncoding
```

### 10.6 命令被手动中断

如果已经显示过 `Thread ID`，先执行：

```powershell
.\.venv\Scripts\specialist-workflow.exe status <Thread-ID>
```

如果状态显示正在等待审批，使用 `inspect` 查看后再 `resume`。如果中断发生在创建检查点之前，则换一个新的 `--thread-id` 重新运行。

## 11. 安全注意事项

- 不要把真实 API Key 写入 README、源码、截图或 Git 提交。
- `.env` 只保存在工作流本机目录，并已被 Git 忽略。
- 第三方中转站可能看到发送给模型的需求、代码差异和文档，不要用于不能交给第三方的私有代码或数据。
- Agent 生成的代码必须经过 `inspect` 和人工审批，不能只看模型结论。
- 工作流不会自动推送远程仓库，也不会自动合并主分支。
- 曾经在聊天或截图中完整暴露过的 API Key 应立即在中转站后台撤销并重新生成。

## 12. 最短操作流程

日常使用只需记住以下四步：

```powershell
# 1. 提交新任务
.\.venv\Scripts\specialist-workflow.exe run --thread-id "唯一任务编号" --repo "C:\目标项目" --request "明确需求" --test-command "测试命令"

# 2. 查看状态和修改
.\.venv\Scripts\specialist-workflow.exe status "唯一任务编号"
.\.venv\Scripts\specialist-workflow.exe inspect "唯一任务编号"

# 3. 批准或拒绝
.\.venv\Scripts\specialist-workflow.exe resume "唯一任务编号" --approve

# 4. 回到目标项目，人工合并 status 中显示的 branch_name
git merge "agent/实际任务ID"
```
