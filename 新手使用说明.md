# 专项多 Agent 工作流：新手使用手册

这套工具可以把一条软件开发需求交给多个 AI 角色依次处理，并在真正提交代码前停下来，等待你检查和批准。

它最重要的安全边界是：

- AI 在独立的 Git 分支和工作区中修改代码，不直接改动正式分支。
- 自动测试和代码审查通过后，仍然必须由你决定批准或拒绝。
- 批准后只会创建本地 Git 提交，不会自动合并到主分支，也不会上传 GitHub。

## 一、先记住整个流程

```text
准备目标项目
    ↓
提交开发需求（run）
    ↓
AI 制定计划、修改代码、运行测试、更新文档、审查代码
    ↓
你查看状态和修改（status + inspect）
    ↓
批准或拒绝（resume --approve / --reject）
    ↓
批准后手动合并到正式项目（git merge）
    ↓
再次运行测试或构建
```

日常使用时，主要记住这五个命令：

```text
run → status → inspect → resume --approve → git merge
```

## 二、几个常用名词

| 名称 | 通俗解释 |
| --- | --- |
| 工作流程序 | 负责调度多个 AI、运行测试和保存任务状态的工具 |
| 目标项目 | 你希望 AI 修改的 Git 项目 |
| Thread ID | 你为本次任务起的唯一编号，用于查询和恢复任务 |
| Agent 分支 | AI 实际修改代码的独立 Git 分支，形如 `agent/xxxxxxxxxxxx` |
| Worktree | Agent 的隔离工作目录，默认位于 `C:\agent-runtime\worktrees` |
| 主分支 | 正式开发分支，通常叫 `main`，也可能叫 `master` 或其他名称 |

请特别注意：Thread ID 和 Agent 分支名不是同一个东西。

- Thread ID 由你在 `--thread-id` 中填写，例如 `milling-fix-004`。
- Agent 分支名由程序生成，例如 `agent/a1b2c3d4e5f6`。
- 合并时必须使用 `status` 显示的真实 `branch_name`，不要根据 Thread ID 猜分支名。

## 三、当前电脑上的目录

工作流程序：

```text
C:\Users\win11\Documents\specialist-agent-workflow
```

当前示例目标项目：

```text
C:\Users\win11\Documents\Codex\2026-05-31\cai-aa-text-to-cae-https\work\text-to-cae
```

Agent 隔离工作区：

```text
C:\agent-runtime\worktrees
```

任务状态数据库：

```text
C:\Users\win11\.specialist-agent-workflow\checkpoints.sqlite
```

目标项目只需要是本机 Git 仓库，不需要先上传到 GitHub。

## 四、首次使用：安装和检查

如果工作流已经安装并且 `doctor` 正常，可直接跳到第五节。

### 4.1 检查基础工具

打开 PowerShell，依次运行：

```powershell
git --version
python --version
codex --version
```

要求：

- Git 可以正常显示版本。
- Python 版本为 3.11 或更高。
- Codex CLI 可以正常显示版本。

### 4.2 安装工作流

```powershell
Set-Location "C:\Users\win11\Documents\specialist-agent-workflow"
Set-ExecutionPolicy -Scope Process Bypass
.\install.ps1
```

`Set-ExecutionPolicy -Scope Process Bypass` 只影响当前 PowerShell 窗口，关闭窗口后会失效。

### 4.3 检查配置

```powershell
.\.venv\Scripts\specialist-workflow.exe doctor
```

正常情况下会显示：

- Base URL；
- 规划、编码、文档和审查所使用的模型；
- Codex CLI 路径；
- Worktree 和 Codex 运行目录；
- `api_key: available (hidden)`。

`doctor` 不应显示真实 API Key。如果命令没有报错，说明基本配置可以使用。

### 4.4 需要单独配置 API 时

工作流默认尝试复用本机 Codex 的登录和配置。如果不能复用，可将 `.env.example` 复制成 `.env`，再填写：

```dotenv
AI_API_KEY=你的完整密钥
AI_BASE_URL=https://你的服务地址/v1
AI_API_MODE=auto
```

不要把真实 API Key 写进 README、`.env.example`、源代码或 Git 提交。`.env` 已被当前项目的 `.gitignore` 忽略。

## 五、每次运行前：准备目标项目

这一步非常重要。工作流要求目标项目：

- 是一个有效的 Git 仓库；
- 至少已经有一个 Git 提交；
- 当前没有未提交或未跟踪的文件；
- 当前分支就是本次任务应该基于的分支。

进入目标项目：

```powershell
Set-Location "C:\Users\win11\Documents\Codex\2026-05-31\cai-aa-text-to-cae-https\work\text-to-cae"
```

查看当前分支和项目状态：

```powershell
git branch --show-current
git status
git log -1 --oneline
```

通常应看到：

```text
nothing to commit, working tree clean
```

如果任务应从 `main` 开始，可先运行：

```powershell
git switch main
git status
```

如果项目的正式分支不叫 `main`，请换成实际分支名。工作流会从运行 `run` 时的当前 `HEAD` 创建 Agent 分支，所以开始前必须确认基础分支正确。

如果 `git status` 显示有修改，不要随意删除。请先确认这些修改的用途，再选择提交、暂存或自行处理。

## 六、提交一个新任务

先回到工作流目录：

```powershell
Set-Location "C:\Users\win11\Documents\specialist-agent-workflow"
```

建议先设置两个变量，后面的查询命令可以直接复用：

```powershell
$ThreadId = "milling-fix-004"
$Repo = "C:\Users\win11\Documents\Codex\2026-05-31\cai-aa-text-to-cae-https\work\text-to-cae"
```

提交任务：

```powershell
.\.venv\Scripts\specialist-workflow.exe run `
  --thread-id $ThreadId `
  --repo $Repo `
  --request "修复 milling-3d 仿真结果网格加载时出现 400 Bad Request 的问题，并更新相关测试和文档" `
  --test-command "npm.cmd --prefix viewer run build"
```

PowerShell 换行符是反引号 ``` ` ```。反引号后面不能再有空格，否则命令可能提前结束。也可以把整个命令写成一行。

### 6.1 参数是什么意思

| 参数 | 是否必填 | 作用 |
| --- | --- | --- |
| `--thread-id` | 建议填写 | 本次任务的唯一编号，用于后续 `status`、`inspect` 和 `resume` |
| `--repo` | 必填 | 目标 Git 项目的完整目录 |
| `--request` | 必填 | 希望 AI 完成的开发任务 |
| `--test-command` | 建议填写 | 修改后必须通过的测试或构建命令 |

每次新任务都要更换新的 Thread ID，例如：

```text
milling-fix-004
viewer-feature-001
login-fix-001
```

建议只使用英文字母、数字和连字符。不要对一个已经运行过的 Thread ID 再次执行 `run`。

如果不填写 `--thread-id`，程序会自动生成随机编号，但手动填写更容易查询。

### 6.2 怎样写清楚任务需求

一条好需求最好包含四部分：

1. 当前问题是什么；
2. 期望结果是什么；
3. 哪些内容可以或不可以修改；
4. 最后用什么命令验收。

示例：

```text
修复 milling-3d 仿真结果网格加载时出现 400 Bad Request 的问题。
保留其他案例现有的加载行为，不修改 API 对外字段。
补充能够覆盖该问题的测试，并更新 README 中的使用说明。
最终必须通过 viewer 构建。
```

需求越具体，AI 修改无关内容的概率越低。

### 6.3 测试命令怎样填写

常见示例：

| 项目类型 | 测试或构建命令 |
| --- | --- |
| 当前 viewer 项目 | `npm.cmd --prefix viewer run build` |
| Python | `python -m pytest` |
| 根目录含 `package.json` 的 Node.js | `npm.cmd test -- --run` |
| Rust | `cargo test` |
| Go | `go test ./...` |

省略 `--test-command` 时，工作流会根据目标项目根目录中的 `pyproject.toml`、`pytest.ini`、`package.json`、`Cargo.toml` 或 `go.mod` 自动判断。

如果测试配置位于子目录，例如当前项目的 `viewer`，应明确填写测试命令。

## 七、等待 AI 自动执行

`run` 之后，工作流会依次执行：

1. `gpt-5.6-terra` 分析需求并制定计划；
2. Codex CLI 使用 `gpt-5.6-sol` 修改代码和测试；
3. 本机运行真实的测试或构建命令；
4. `claude-sonnet-4-6` 根据代码差异更新文档；
5. `gpt-5.6-luna` 独立审查代码、测试和文档；
6. 测试或审查失败时，在允许次数内自动修改并重试；
7. 全部通过后暂停，等待你的批准。

模型名称是当前默认配置，可以通过工作流目录中的 `.env` 调整。

执行可能需要几分钟。模型调用或测试运行期间不要关闭 PowerShell。正常到达人工审批阶段后，会看到类似提示：

```text
Workflow paused for human approval
```

## 八、查看任务状态和代码修改

确保当前目录是工作流目录：

```powershell
Set-Location "C:\Users\win11\Documents\specialist-agent-workflow"
$ThreadId = "milling-fix-004"
```

### 8.1 查看状态

```powershell
.\.venv\Scripts\specialist-workflow.exe status $ThreadId
```

重点查看：

- `status`：当前阶段；
- `branch_name`：真正的 Agent 分支名；
- `worktree_path`：AI 实际修改代码的目录；
- `test_report.passed`：测试是否通过；
- `test_report.command`：实际运行的测试命令；
- `review.passed`：独立审查是否通过；
- `review.findings`：审查发现的问题。

常见状态：

| 状态 | 含义 |
| --- | --- |
| `planning` | 正在制定计划 |
| `coding` | 正在修改代码 |
| `testing` | 正在测试 |
| `documenting` | 正在更新文档 |
| `reviewing` | 正在审查 |
| `retrying` | 测试或审查未通过，准备重试 |
| `awaiting_approval` | 已通过自动流程，等待人工批准 |
| `completed` | 已批准并在 Agent 分支创建提交 |
| `rejected` | 已人工拒绝 |
| `failed` | 达到最大重试次数后仍未通过 |

### 8.2 查看完整修改

```powershell
.\.venv\Scripts\specialist-workflow.exe inspect $ThreadId
```

`inspect` 显示待批准的完整 Git 差异。批准前至少检查：

- 是否真正解决了原问题；
- 是否修改了不相关的文件；
- 测试或构建是否通过；
- Reviewer 是否报告中高风险问题；
- 新增测试是否覆盖了本次问题；
- 文档是否和代码行为一致；
- 是否意外包含密钥、个人数据或大文件。

不要只看 `review.passed`，人工检查仍然是流程的一部分。

## 九、批准或拒绝

只有状态处于 `awaiting_approval` 时，才应执行批准或拒绝。

### 9.1 确认修改正确：批准

```powershell
.\.venv\Scripts\specialist-workflow.exe resume $ThreadId --approve
```

批准后，工作流会：

- 把当前修改提交到 Agent 分支；
- 在状态中记录提交哈希；
- 将任务状态设为 `completed`。

批准后，工作流不会：

- 自动合并到 `main`；
- 自动推送到 GitHub；
- 自动删除 Agent 分支或 Worktree。

### 9.2 不接受修改：拒绝

```powershell
.\.venv\Scripts\specialist-workflow.exe resume $ThreadId --reject
```

拒绝后，修改仍会以未提交状态保留在对应的 Worktree 中，方便排查，但不会进入正式项目。

`--approve` 和 `--reject` 必须二选一，不能同时使用。

## 十、把批准的修改合并到正式项目

先用 `status` 找到真实分支名：

```powershell
.\.venv\Scripts\specialist-workflow.exe status $ThreadId
```

假设输出中显示：

```text
branch_name: agent/a1b2c3d4e5f6
```

回到目标项目：

```powershell
Set-Location "C:\Users\win11\Documents\Codex\2026-05-31\cai-aa-text-to-cae-https\work\text-to-cae"
```

确认正式项目当前没有其他修改：

```powershell
git status
```

切换到正式分支并合并：

```powershell
git switch main
git merge "agent/a1b2c3d4e5f6"
```

把示例分支名替换为 `status` 显示的真实 `branch_name`。如果正式分支不叫 `main`，请使用实际名称。

合并后再次运行项目验证：

```powershell
npm.cmd --prefix viewer run build
```

只有这次验证也成功，才算完整结束。如果发生 Git 合并冲突，不要盲目继续，应先检查冲突内容。

工作流本身不会执行 `git push`。是否上传远程仓库由你另行决定。

## 十一、可以直接复制的完整模板

```powershell
# 1. 准备目标项目
$Repo = "C:\目标项目完整路径"
Set-Location $Repo
git branch --show-current
git status

# 2. 回到工作流目录并提交新任务
Set-Location "C:\Users\win11\Documents\specialist-agent-workflow"
$ThreadId = "唯一任务编号"

.\.venv\Scripts\specialist-workflow.exe run `
  --thread-id $ThreadId `
  --repo $Repo `
  --request "清楚描述问题、期望结果、修改范围和验收要求" `
  --test-command "项目的测试或构建命令"

# 3. 查看自动执行结果和完整修改
.\.venv\Scripts\specialist-workflow.exe status $ThreadId
.\.venv\Scripts\specialist-workflow.exe inspect $ThreadId

# 4. 二选一：批准或拒绝
.\.venv\Scripts\specialist-workflow.exe resume $ThreadId --approve
# .\.venv\Scripts\specialist-workflow.exe resume $ThreadId --reject

# 5. 批准后回到目标项目，合并 status 显示的真实 branch_name
Set-Location $Repo
git switch main
git merge "agent/实际分支名"

# 6. 再次运行测试或构建
项目的测试或构建命令
```

## 十二、常见问题

### 12.1 `doctor` 报错

先分别检查：

```powershell
git --version
python --version
codex --version
```

然后检查 API Key 和 Base URL 是否已通过 Codex 配置或工作流目录中的 `.env` 提供。

### 12.2 `FileNotFoundError: [WinError 2]`

通常表示 PowerShell 找不到 Git、Python、Codex 或测试命令。先用对应的 `--version` 命令确认工具是否在 `PATH` 中。

如果 Git 已安装在默认位置，但当前窗口尚未识别，可临时执行：

```powershell
$env:Path = "C:\Program Files\Git\cmd;$env:Path"
git --version
```

### 12.3 `The source repository has uncommitted changes`

目标项目存在未提交或未跟踪文件。进入目标项目并查看：

```powershell
git status
```

确认这些文件后再决定提交或暂存。不要为了启动工作流而直接丢弃尚未确认的个人修改。

### 12.4 `Repository path does not exist`

检查 `--repo` 路径：

```powershell
Test-Path "C:\目标项目完整路径"
```

只有返回 `True` 才表示目录存在。带空格或中文的路径应放在英文双引号中。

### 12.5 `Could not detect tests`

工作流没有在目标项目根目录识别出测试框架。重新使用新的 Thread ID 运行，并显式添加：

```powershell
--test-command "正确的测试或构建命令"
```

### 12.6 `No checkpoint found for thread ...`

通常是 Thread ID 输入错误。检查大小写、数字和连字符是否与运行任务时完全一致。

### 12.7 测试或审查最终失败

工作流默认最多自动重试两次。达到上限后，状态会变成 `failed`，修改仍保留在 Agent 分支对应的 Worktree 中，但不会进入审批阶段。

先用下面的命令查看原因：

```powershell
.\.venv\Scripts\specialist-workflow.exe status $ThreadId
.\.venv\Scripts\specialist-workflow.exe inspect $ThreadId
```

修正需求、测试命令或外部环境后，使用新的 Thread ID 提交新任务。

### 12.8 PowerShell 命令换行失败

反引号必须是该行最后一个字符，后面不能有空格。最简单的排查方法是把命令临时写成一行。

### 12.9 中文显示乱码

在当前 PowerShell 窗口执行：

```powershell
chcp 65001
[Console]::InputEncoding = [System.Text.UTF8Encoding]::new()
[Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()
$OutputEncoding = [Console]::OutputEncoding
```

### 12.10 合并时发生冲突

说明正式分支在 Agent 工作期间发生了重叠修改。先运行 `git status` 查看冲突文件，人工确认正确内容后再继续。不了解冲突处理时，不要随意删除文件或强制覆盖。

## 十三、安全注意事项

- 不要在需求、源码、文档、截图或 Git 提交中暴露真实 API Key。
- 第三方模型服务可能看到发送给模型的需求、代码差异和文档内容，不要处理不能交给第三方的私有代码或敏感数据。
- 开始任务前必须确认目标项目状态干净，避免混入个人修改。
- 批准前必须运行 `status` 和 `inspect`。
- 自动测试通过不等于一定没有问题，仍需人工检查核心功能。
- `--approve` 只是在 Agent 分支提交，不等于合并。
- 工作流不会自动上传 GitHub，也不会自动进入正式分支。
- 不要在审批前手动编辑或删除 `C:\agent-runtime\worktrees` 中的目录。

## 十四、最短操作速查

```powershell
Set-Location "C:\Users\win11\Documents\specialist-agent-workflow"
$ThreadId = "新的唯一任务编号"

.\.venv\Scripts\specialist-workflow.exe run `
  --thread-id $ThreadId `
  --repo "C:\目标项目" `
  --request "明确的开发需求" `
  --test-command "测试命令"

.\.venv\Scripts\specialist-workflow.exe status $ThreadId
.\.venv\Scripts\specialist-workflow.exe inspect $ThreadId
.\.venv\Scripts\specialist-workflow.exe resume $ThreadId --approve

# 回到目标项目后，使用 status 显示的真实 branch_name
git switch main
git merge "agent/实际分支名"
```

一句话记忆：

```text
新建 Thread ID → run → 看 status → 看 inspect → approve → 合并真实 branch_name → 再测试
```
